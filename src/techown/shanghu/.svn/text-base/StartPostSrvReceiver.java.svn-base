package techown.shanghu;
/*
 * 开机启动PostServer服务
 */
import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

public class StartPostSrvReceiver extends BroadcastReceiver {
	
//	private final String action = "techown.login.do.send.to.apks";
	
	@Override
	public void onReceive(Context context, Intent intent) {
		
		if (intent.getAction().equals(Intent.ACTION_BOOT_COMPLETED)) {
			//启动post service
//			Intent it = new Intent();
//			it.setClass(context, PostService.class);
//			context.startService(it);
//
////			Intent it=new Intent("techown.shanghu.https.PostService");
////			context.startService(it);
//			techown.shanghu.https.Log.Instance().WriteLog("团队：启动post service");
		}
		
	}

	
}